# Node
node_modules/
npm-debug.log

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local

# Orchestrator
orchestrator/*.log
orchestrator/.langgraph/
.langgraph/

# Frontend build artifacts
frontend/node_modules/
frontend/dist/

# Built frontend artifacts. The Pages workflow (.github/workflows/pages.yml)
# builds these on demand and uploads them as a Pages artifact — no need to
# commit. Local `npm run build` puts the output here too; ignore it.
docs/search/

# Builder dry-run output
.dry-run-prompts/

# Verifier transient output
builder/scripts/__fixtures__/*.actual

# Run logs — operational telemetry written by the cloud agent each run.
# Never read by subsequent runs (see LEARNINGS.md for cross-run knowledge).
# No need to version-control these.
data/run-logs/

# Hermes Agent — local-only, never tracked
.hermes/
